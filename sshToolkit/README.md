# SSH 连接管理器

GUI 工具，管理和快速连接 SSH 服务器。

## 功能

- 保存/加载 SSH 连接配置
- 连接验证后自动打开终端
- 支持密码认证

## 使用

```bash
# 安装依赖
pip install paramiko

# 运行
python sshTool.py
```

## 依赖

- Python 3.8+
- paramiko
- tkinter (Python 内置)
